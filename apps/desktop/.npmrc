# npm 11 arborist bug: installing inside apps/desktop/ while a Bun workspace root
# exists above causes resolution failures. --no-workspaces scopes npm to this
# package only and avoids the conflict.
workspaces=false
